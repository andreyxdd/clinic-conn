/*
import { useContext } from 'react';
import { ClientContext } from '../context/ClientContextProvider';
import { IClientContextProps } from '../config/types';

const useClientContext = () => useContext(ClientContext) as IClientContextProps;

export default useClientContext;
*/
export { };
