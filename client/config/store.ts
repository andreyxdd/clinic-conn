/*
import create from 'zustand';
import { IUserStateProps } from './types';

const useStore = create<IUserStateProps>((set) => ({
  isLoggedIn: false,
  setIsLoggedIn: (isLoggedIn: boolean) => set({ isLoggedIn }),
}));

export default useStore;
*/
export { };
